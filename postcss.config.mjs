const config = {
  plugins: ["@tailwindcss/postcss"],
  fontFamily: {
    nova: ['Nova Round"', 'sans-serif'],
    worksans: ['var(--font-work-sans)'],
    novaround: ['var(--font-nova-round)'],
  },
};

export default config;
